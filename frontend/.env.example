# API path the app calls. Keep /api/v1 when using Vite proxy (dev) or Vercel rewrites (prod).
VITE_API_URL=/api/v1

# Dev only: where Vite proxies /api and /uploads. Omit for local backend (localhost:5000).
# VITE_PROXY_TARGET=https://umeed-e-zindagi-server-zar-cloud.vercel.app

VITE_APP_NAME=Umeed-e-Zindagi Institute
